# Суррогат: что сделано, что осталось, как продолжить

Документ для того, кто будет доводить суррогат на более мощном железе. Здесь только факты и замеры, без планов и предположений.

Состояние на 18.08.2026. Описанное ниже лежит в `main` репозитория `aios-hack/aios`, кроме работы по задаче 34 — она в ветке `feat/surrogate/34-700`.

Датасет посчитан целиком и лежит в отдельном репозитории `aios-hack/dataset-700` — см. §2.

---

## 1. Что уже готово и работает

Инфраструктура вокруг суррогата написана, протестирована и влита. **Не хватает обучаемой нейросетевой реализации, достаточного датасета и checkpoint модели.** Компоненты поиска и внешней проверки уже существуют, но без реального предиктора их протоколы можно проверить только на контрактных тестовых реализациях.

| Компонент | Где | Состояние |
|---|---|---|
| Генератор датасета | `bridge/dataset.py`, `bridge/dataset_plan.py` | готов, проверен настоящими прогонами OPM |
| Фичеризатор (вход модели) | `surrogate/features.py` | готов, вход строится из одного `Schedule` |
| Адаптер выхода модели | `surrogate/adapter.py` | готов, отдаёт два контрактных типа отклика |
| Базовая линия CRM | `surrogate/crm.py` | **готова и измерена**, см. §3 |
| Детектор выхода за обучающий диапазон | `surrogate/ood.py` | готов; домен строится по обучающей выборке, `ood_score` обязателен вместе с прогнозом |
| Метрики качества | `surrogate/metrics.py` | готовы: ранжирование ЧДД, top-k, regret, обводнённость и состояния |
| Runner + кеш прогонов | `bridge/runner.py`, `bridge/cache.py` | готовы, кеш по тройке хешей |
| Загрузчик отклика | `bridge/response_loader.py` | готов, разбирает SMSPEC/UNSMRY |
| Интерфейс оптимизатора | `optimizer/interface.py` | готов, θ → структура с `feasible` |
| Оптимизатор | `optimizer/search.py` | готов, выбран CMA-ES |
| Внешняя OPM-верификация | `bridge/verification.py` | готов цикл с областью доверия, top-k, дообучением и `self_consistent` |
| Калибровка критерия сходимости | `bridge/convergence.py` | готовы три семейства критериев и замер на настоящей истории; победитель не назначается на синтетике |

В ветке `feat/surrogate/34-700` к этому добавлены сама модель и обвязка обучения:

| Компонент | Где | Состояние |
|---|---|---|
| Модель суррогата | `surrogate/model.py` | `TrajectorySurrogate` на torch, шесть величин раздельно |
| Обучение | `surrogate/train.py` | готово, разбиение и ранняя остановка по patience |
| Цикл на три стадии | `surrogate/cycle.py` | пилот 200, расширение 500, обучение на 700, состояние в `data/cycle.json` |
| Ролевой таймлайн | `surrogate/schedule_roles.py` | роль скважины с учётом перевода в нагнетание |
| Дашборд прогресса | `surrogate/dashboard.py` | HTTP Basic, пароль через `AIOS_DASHBOARD_PASSWORD` |

**Чего нет:** обученного checkpoint. Ни одна эпоха не прошла — обучение падает на инициализации, см. §6. Поэтому реальных результатов приёмки, OOD и калибровки по-прежнему нет: задачи 35, 36, 38, 39 и 40 структурно реализованы, но их численные результаты появятся только после первого успешного обучения.

---

## 2. Датасет: сколько посчитано и сколько осталось

### Замеры времени, не оценки

| Условие | Время одного прогона Model_Z |
|---|---|
| Свободная машина | **513 с** (8.5 мин) |
| Машина занята параллельными задачами | **1749–1857 с**, среднее 1823 |

Прогон — это полный горизонт: 371 отчётный шаг, 103 скважины, OPM Flow в контейнере `openporousmedia/opmreleases:latest`.

### Какие данные доступны

Датасет посчитан **полностью**: 732 прогона, все со статусом `OK`, ни одного отказа Flow. Считался 16–17 августа 2026 года на 32-ядерной машине, около 200 машино-часов.

| Каталог | Уникальных расписаний | Прогонов |
|---|---|---|
| `dataset-main` | 200 | 200 |
| `dataset-extra-500` | 499 | 532 |

Семейства сценариев по строкам манифеста: LEVELS 1605, UNREACHABLE 500, SHUTDOWN 436, CONVERSION 181, BASELINE 10.

Машина, на которой это считалось, погашена. Данные лежат в приватном репозитории **`aios-hack/dataset-700`** — там же базовый прогон, манифесты с хешами, планы, деки, состояние цикла и снимок окружения сервера. Правило «`data/` вне git» для основного репозитория остаётся в силе; отдельный репозиторий заведён потому, что пересчёт стоит 200 машино-часов.

Продолжать так:

```bash
git clone https://github.com/aios-hack/dataset-700.git
cp -R dataset-700/dataset-main dataset-700/dataset-extra-500 aios/data/
```

Кеш ключуется тройкой хешей (§4.5 контрактов), поэтому ни один перенесённый прогон не будет посчитан повторно.

### Сколько нужно на самом деле

Оценка «минимум 200–400 прогонов» делалась из размерности задачи: 224 интервала × 103 скважины на пару «расписание → отклик». Посчитано 732, то есть верхняя граница перекрыта вдвое. Достаточно ли этого фактически — станет ясно только по метрикам приёмки против CRM, потому что ни одного обучения ещё не завершилось.

---

## 3. Базовая линия CRM — порог, который суррогат обязан бить

Обучена на настоящем отклике базового прогона. **Подгонка занимает 8 секунд.**

Разрез оси времени: 168 интервалов на обучение, **56 отложенных** на замер. Метрики приёмки считаются только на отложенной части.

| | R² | Spearman | MAE | медианная отн. ошибка |
|---|---|---|---|---|
| **Отложенная (приёмка)** | **0.9482** | **0.9787** | 52.7 м³ | 4.88% |
| Обучающая (диагностика) | 0.9694 | 0.9815 | 34.5 м³ | 3.75% |

Разрыв train−holdout 0.021 — переобучения нет. Фонд: 48 добывающих × 40 нагнетательных.

### Два результата, которые не надо переоткрывать

**Ограничение знака коэффициентов несущее.** Свободная подгонка даёт train R² 0.99 при holdout R² **−5.3** — чистое запоминание. Запрет отрицательной аллокации (`f ≥ 0`) поднимает holdout до +0.87.

**Материальный баланс накладывается в ходе подгонки, а не после.** Без ограничения 17 из 41 нагнетательной нарушают баланс (максимум суммы 4.14). Пересчёт коэффициентов уже сошедшейся модели роняет R² до **−2.56**, потому что свободный член остаётся подогнанным под старые значения.

### Чего эти метрики НЕ доказывают

Это важно, чтобы не сравнить суррогат с CRM некорректно:

- метрики измерены **при одной программе закачки** — показан перенос с ранних интервалов на поздние, а не с одного сценария на другой, а оптимизатору нужно именно второе;
- **ранговой корреляции по ЧДД здесь нет и быть не может**: сценарий один, ранжировать нечего. Приведённая Spearman — по объёмам жидкости, это другая величина. Полноценный порог появится только на датасете;
- отложенная часть систематически «богаче» обучающей (закачка растёт 50.1k → 58.2k м³/мес, фонд нагнетательных 27 → 41), часть holdout-качества — экстраполяция тренда;
- 22 скважины с неполной историей исключены из подгонки;
- **CRM моделирует жидкость, а деньги сидят в обводнённости** — как базовая линия по нефти он слаб по построению.

---

## 4. Как продолжить: конкретные шаги

### Шаг 1. Развернуть окружение и данные

```bash
git clone https://github.com/aios-hack/aios.git
git clone https://github.com/aios-hack/docs.git         # соседним каталогом, код ищет ../docs
git clone https://github.com/aios-hack/dataset-700.git  # 732 посчитанных прогона
cd aios
git checkout feat/surrogate/34-700
python3 -m venv .venv && .venv/bin/pip install -e ".[dev,ml]"
cp -R ../dataset-700/dataset-main ../dataset-700/dataset-extra-500 data/
```

Экстра `ml` тянет torch и numpy; для обучения на CPU этого достаточно, GPU не требуется. `docker pull openporousmedia/opmreleases:latest` нужен только если досчитывать датасет или гонять внешнюю верификацию — для обучения на готовых прогонах Docker не нужен.

Если датасет всё-таки надо расширять, запуск генерации (скрипт `build_dataset.py` в корне, если его нет — см. §5):

```bash
AIOS_DATASET_WORKERS=8 AIOS_DOCS_ROOT=/путь/к/docs python build_dataset.py
```

Число воркеров подбирается под железо. **Важно:** Flow внутри контейнера сам многопоточный, поэтому воркеров имеет смысл ставить примерно `ядра / 2`, а не по числу ядер — иначе прогоны отбирают ядра друг у друга. Замер: на 16 ядрах при конкуренции прогон замедлялся с 513 до 1823 с, то есть в 3.5 раза.

Для объёма 200–400 прогонов правьте `PlanConfig` в `bridge/dataset_plan.py` — параметры `n_level_scenarios`, `n_unreachable_scenarios`, `n_shutdown_scenarios`, `n_conversion_scenarios`.

### Шаг 2. Обучить модель (задача 34)

Требования контракта (`docs/context/08_contracts.md` §5, `07_concept.md` §5.1):

- предсказывать **три величины раздельно**: факт добычи жидкости, обводнённость, приёмистость. **Дебит нефти напрямую не предсказывать** — он выводится как `q_нефти = q_жидкости × (1 − обводнённость)`;
- вход — только целевые накопления с 1994 плюс λ своего окна (`surrogate/features.py` это уже строит);
- выход — два контрактных типа, `IntervalResponse[224]` и `StateAtDate[371]` (`surrogate/adapter.py` это уже принимает);
- детектор выхода за обучающий диапазон уже реализован в `surrogate/ood.py`; после формирования train-split нужно построить `TrainingDomain` и возвращать `ood_score` вместе с каждым прогнозом;
- модель должна иметь стабильный `version`/идентификатор checkpoint, который внешний цикл использует при переоценке после дообучения.

**Главная ловушка** (§5.1.1): модель, обученная «управление → дебит» в лоб, выучит тождество `дебит ≈ целевая уставка`, покажет отличный MAE, а оптимизатор поверх неё скажет «поднять всё» и обвалит давление. **Суррогат обязан моделировать недостижимость цели** — то есть случаи, когда скважина упирается в предел по забойному давлению и не добирает заданный режим. В датасете такие сценарии есть специально (семейство «недостижимые уставки»).

Численное подтверждение, что ловушка реальна: на базовом прогоне **11 226 недоборов цели, из них 11 166 в режиме BHP-лимита** при нулевых нарушениях самих пределов давления.

### Шаг 3. Выполнить реальную приёмку готовыми метриками (задача 36)

Оптимизировать на **ранжирование, а не на MAE** (§5.2). Реализация в `surrogate/metrics.py` считает ранговую корреляцию по ЧДД, precision@k, regret на top-k, обводнённость и обязательные метрики `StateAtDate`.

Порог приёмки: **суррогат обязан бить CRM по ранговой корреляции**, иначе отвергается. Функция сравнения готова — `compare_to_baseline` в `surrogate/crm.py`.

---

## 5. Технические детали окружения

**Зависимости.** numpy и scipy в проекте отсутствуют — CRM написан на stdlib. Если для суррогата нужны тяжёлые библиотеки, добавлять их своей строкой в `pyproject.toml`, не трогая чужие.

**Пути к данным.** Дек и эталонный расчётчик ищутся через `AIOS_DOCS_ROOT`, сохранённый базовый прогон — через `AIOS_BASE_RUN_DIR`. Резолверы в корневом `conftest.py`.

**Скрипт генерации** (`build_dataset.py`) в git не отслеживается. Если его нет, тот же запуск воспроизводится через публичные функции:

```python
from pathlib import Path

import conftest
from bridge.dataset import DatasetGenerator
from bridge.dataset_plan import build_plan

generator = DatasetGenerator(conftest.model_z_dir(), Path("data/dataset-main"),
                             max_workers=8, timeout_seconds=7200.0)
plan = build_plan(generator.base_schedule(), seed=20260816)
report = generator.build(plan)
```

**На Windows** обязательно `sys.stdout.reconfigure(encoding="utf-8")` — иначе русский вывод падает на cp1252.

**Правила проекта, которые нельзя нарушать:**

- моков нет: компонент, который не может посчитать, бросает исключение, а не возвращает `0.0` или случайный тензор;
- синтетические данные допустимы только для проверки формы, **никогда в метриках качества**, и помечаются флагом в метаданных;
- `data/` в git не кладётся, адресуется хешами;
- `contracts/` правит только интегратор.

---

## 6. Где обучение стоит прямо сейчас

Четыре запуска `surrogate.cycle --resume-extra` 17 августа, все упали, каждый дальше предыдущего:

| Время UTC | Ошибка |
|---|---|
| 16:09 | `AttributeError: '_WellTimeline' object has no attribute 'role'` |
| 18:00 | `FeatureError: '102': SET_LRAT применён не к PROD` |
| 18:48 | `SurrogateModelError: отрицательная/нечисловая цель ('102', 0)` |
| 20:07 | `SurrogateModelError: отрицательная цель ('102', 24) oil_mass_delta=-0.0015047038505144883` |

Первые две починены: роль вынесена в `surrogate/schedule_roles.py`, потому что скважина `102` переводится из добывающей в нагнетательную, а загрузчик отклика роль не восстанавливает.

**Последняя не починена.** Это 1.5 грамма нефти за месяц — физический ноль. UNSMRY хранит накопленные величины 4-байтными float, относительная точность около `1e-7`; при накопленной массе порядка `1e4` т шум вычитания как раз и даёт `1e-3` т. В `_targets` (`surrogate/model.py`) порог отсечки абсолютный, `-1e-3`, и значение чуть за него вылезло. Допуск должен быть относительным к величине накопления, а не константой.

Заодно стоит перенести проверку целей вперёд, до фичеризации: сейчас каждая неудачная попытка сжигает 46 минут на загрузку 700 откликов и падает на первой секунде сборки тензоров.

Контекст фичеризации от последней попытки сохранён — `data/model-task34-700/feature_context.json`, он же в `dataset-700`. Файлов `model.pt` и `training_report.json` не существует: ни одна эпоха не начиналась.

---

## 7. Что даёт суррогат и что будет без него

**Формально суррогат необязателен** (`docs/context/06_open_questions.md` §2), но влияет на рейтинг и на решение в спорной ситуации о выходе в финал.

**Без него система работоспособна:** есть CRM как базовая линия, есть правила R0–R7, есть иерархия агентов, есть экономика, сходящаяся с эталонным расчётчиком организаторов до машинного нуля. ЧДД базового сценария посчитан и защитим — **11 886 млн руб** на настоящем прогоне OPM.

**С ним появляется** возможность использовать уже реализованные CMA-ES и внешний цикл верификации на настоящих быстрых прогнозах: перебирать тысячи вариантов, отбрасывать OOD-кандидатов и отправлять в OPM только top-k. Пока модели нет, эти компоненты существуют как проверенная инфраструктура, но не образуют рабочего оптимизационного тракта на данных Model_Z.
