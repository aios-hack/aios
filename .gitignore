# Артефакты прогонов и датасеты — вне git, адресуются хешами
/data/

# Выход CLI: wells_schedule.inc и npv.json воспроизводятся командами emit и npv,
# держать их в git — второй источник истины про одно и то же число
/out/

# Репозиторий документации — отдельный git, склонирован внутрь рабочей копии
docs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Frontend
node_modules/
dist/
build/

# Локальное окружение
.env
.env.local
.DS_Store
.impeccable/hook.cache.json
**/.impeccable/hook.cache.json
