# Что нужно посчитать по суррогату

Запросы на машинное время от того, кто разбирал модель задачи 34.
Первая редакция 20.08, обновлено 21.08 по итогам G9, G10 и собственных
замеров воспроизводимости датасета.

Каждый пункт — что запустить, зачем, сколько стоит и что я с результатом
сделаю. Закрытые пункты не удаляются, а помечаются: важно, что именно
оказалось неверным.

---

## 0. Числа, из которых всё следует

Замерено по `dataset-700/model-task34-700/training_report.json`, то есть по
собственному тестовому набору опубликованного чекпоинта (105 сценариев).

| величина | значение |
|---|---|
| эффективный шум ЧДД | **2.10e8 ₽** |
| зазор между чемпионом и вторым местом | 8.72e7 ₽ |
| наклон регрессии предсказанного на фактический | **0.307** |
| разброс предсказаний / разброс фактический | 0.398 |
| Spearman на тестовом сплите | 0.737 |
| параметров модели / строк обучения | 41 078 / 11 305 280 |

Из этого следуют два вывода, на которых держится всё остальное.

**Выигрыш CMA-ES в 658 млн — верхний хвост собственной ошибки модели.**
658e6 / 2.10e8 = **3.13σ**, что отвечает примерно 130 эффективно независимым
оценкам — рабочий диапазон CMA-ES с коррелированными выборками. Настоящие
19 млн — это 0.09σ, такой сигнал модель не разрешает в принципе.

**Отставание в 1.07 млрд от базового — это 5σ.** Сигнал уверенно выше шума,
значит диагностируется. G9 это подтвердил: одна статья, выручка, и одна
причина — недокачка воды на 35.6%.

---

## 0.5. Результаты 22.08: три гипотезы проверены, ни одна не подтвердилась

Стенд собран верно: 700 прогонов из кеша, ноль симуляций OPM, **11 305 280
строк обучения** — ровно как `target_rows` в опубликованном отчёте, сплит
490/105/105, сид 20260817. Девять конфигураций на одном сплите и одних
нормативах, оценка по ЧДД на 105 тестовых сценариях.

| рука | наклон | сжатие | Spearman | p@1 | чемпион |
|---|---|---|---|---|---|
| база | +0.166 | 0.267 | **+0.526** | 1.00 | 1 |
| watercut | +0.146 | 0.276 | **+0.526** | 1.00 | 1 |
| ширина 512 | +0.167 | 0.293 | **+0.522** | 1.00 | 1 |
| ширина 1024 | +0.180 | 0.311 | **+0.528** | 1.00 | 1 |
| деньги + отбор по деньгам | −0.155 | 0.392 | +0.217 | 0.00 | 5 |
| деньги | −0.276 | 0.436 | +0.200 | 0.00 | 6 |
| деньги + watercut | −0.268 | 0.431 | +0.196 | 0.00 | 7 |
| деньги + ширина 512 | −0.273 | 0.433 | +0.183 | 0.00 | 8 |
| деньги + watercut + отбор | −0.308 | 0.475 | +0.183 | 0.00 | 7 |

**Денежное взвешивание лосса вредит — во всех пяти вариантах.** Spearman
падает с 0.53 до 0.18–0.22, `p@1` с единицы до нуля, `regret@1` растёт с нуля
до 1.2–1.8 млрд. Это была основная правка ветки `feat/surrogate/34-money-loss`,
и на верном стенде она отклонена. Механизм: вес `|₽/ед|·scale·(1+v)` в
log-пространстве растёт экспоненциально с величиной, несколько десятков узлов
с крупными объёмами забирают почти весь градиент, и модель перестаёт различать
сценарии. Потолок в 50 не спасает. **По умолчанию взвешивание выключено**
(`money_rub_per_unit=()`), в main ничего вредного не попало.

**Контрактная параметризация целей нейтральна.** Spearman ровно тот же 0.526,
`p@1` тот же. Не вредит и не помогает по числам, но выполняет требование §5.1
и делает нефть согласованной с жидкостью по построению, поэтому оставлена
дефолтом в `train.py`.

**Гипотеза о нехватке ёмкости отклонена.** Ширина 128 → 512 → 1024 (сеть в
восемь раз шире) не улучшает ничего: валидационный лосс 5.385e-3 против
5.467e-3 и 5.453e-3, обе широкие руки останавливаются раньше (34 эпохи против
53) и стоят в 3.4 и 10 раз дороже. Это переобучение, а не недообучение. Моё
рассуждение «275 строк на параметр, значит мало» строилось на наблюдении
`best_epoch 78 из 80` — но оно сделано на стенде из 200 сценариев, где данных
действительно не хватало. На 700 база останавливается сама на 43-й эпохе.

**Главное, что из этого следует.** У четырёх рук с равномерным лоссом Spearman
равен 0.522, 0.526, 0.526, 0.528 — при восьмикратном изменении ширины и полной
смене представления целей разброс уходит в третий знак. **Это потолок, и ни
один рычаг внутри модели его не двигает.** Узкое место выше по течению — во
входах.

Самый вероятный кандидат — λ. Мой контекст оценивал матрицу влияния
`estimate_training_lambda` по подвыборке из 120 сценариев (компромисс ради
памяти), то есть выводил связность из откликов. У вас есть **измеренная** λ из
108 прогонов OPM по Плакетту—Берману с устойчивостью 0.993 между независимыми
половинами. Замена оценки на измерение — следующая проверка, и она объясняет
потолок лучше всего, что я перебрал.

**Оговорка о сравнимости с опубликованным чекпоинтом.** У него Spearman 0.737
против моих 0.526, но `p@1` ноль против моей единицы. Отличие стенда одно и
намеренное: λ оценена по 120 сценариям вместо 490. Абсолютные числа поэтому не
переносятся; сравнение девяти рук между собой корректно — сплит, нормативы и
контекст у всех общие.

---

## 1.0. Итог 25.08: ЧДД попадает с точностью 0.52%

### Абсолютная точность: аффинная калибровка по валидации

| рука | b | ошибка до | ошибка после | % от ЧДД |
|---|---|---|---|---|
| ранговый вес 3 | 1.308 | 2.354e+09 | **6.101e+07** | **0.52%** |
| ранговый вес 4 | 0.841 | 9.324e+07 | **6.100e+07** | **0.52%** |
| вес 3, сид 3 | 1.073 | 1.306e+09 | **6.154e+07** | **0.52%** |
| база | 2.484 | 1.273e+08 | 1.422e+08 | 1.20% |

Три независимые руки сходятся к 6.1e7 ₽ при типичном ЧДД 1.18e10. Смещение
падает в 275 раз. Калиброванная ошибка становится **ниже отрыва чемпиона от
второго места** (7.58e7): отношение 0.80 против 2.93 у базы и 2.41 у
опубликованного чекпоинта.

Калибровка не создаёт информации: у базы наклон выходит 2.48, растягивание
разгоняет вместе с сигналом и шум, и ошибка растёт. Это отдельная проверка
того, что у ранговых рук больше сигнала, а не удачнее масштаб.

### Цена: физика по узлам хуже в 2–4 раза

| вес | Spearman | физика, ₽/узел | p@10 |
|---|---|---|---|
| 0 | +0.526 | 94 257 | 0.60 |
| 0.3 | +0.620 | 239 210 | 0.70 |
| 1 | +0.697 | 201 911 | 0.90 |
| 3 | +0.745 | 289 862 | 0.80 |
| 4 | +0.720 | **193 417** | 0.80 |
| 10 | +0.625 | 646 814 | 0.60 |

Плата берётся **один раз за сам факт включения** рангового члена: в диапазоне
весов 0.3–5 физика почти не зависит от веса. Лучший компромисс — вес 4.

### Компромисс неустраним в одной сети

Тёплый старт (физика первой фазой до сходимости, порядок второй с малым lr)
**не работает**: ранг пикует на 4–15-й эпохе и падает, максимум +0.778 против
+0.889 при обучении с нуля. Стартуя с весов, выученных под регрессию, сеть не
перестраивается — она возвращается к поведению модели без рангового члена.

Значит под физику и под порядок нужны **разные внутренние представления**, и
второе не получается локальной подстройкой первого. Это объясняет и то, почему
плата не зависит от веса, и то, почему ранг у обычной модели пикует на пятой
эпохе. Одна сеть под две задачи — это выбор одной из них, а не точка на кривой.

**Практический вывод: если нужны и точный прогноз физики, и ранжирование,
честнее держать две модели.** Каждая стоит 10–20 минут обучения на готовых
тензорах.

### Spearman — не та метрика для этой задачи

Взвешивание пар по важности позиции (вес пары равен разнице ценностей
`1/(1+позиция)`, как в NDCG):

| | Spearman | p@5 | чемпион |
|---|---|---|---|
| все пары равны | +0.745 | 0.60 | 6 |
| верхушка важнее | +0.678 | 0.60 | **3** |

Spearman падает на 0.067, потому что меряет весь порядок вместе с хвостом,
который мы намеренно перестали оптимизировать. Чемпион при этом поднимается с
шестого места на третье — а именно это определяет длину шортлиста и стоимость
верификации. Метрику приёмки стоит выбрать сознательно: Spearman и место
чемпиона тянут в разные стороны.

### Чего не хватает

Кросс-валидация по нескольким сплитам. Три сида одной конфигурации дали
±0.010, то есть обучение устойчиво, но тестовый набор во всех трёх один и тот
же — 105 сценариев. Прогоны в этой сессии систематически обрывались, довести
не удалось.

---

## 0.9. Результат 24.08: различающий шум упал в 2.2 раза

Потолок Spearman 0.53, державшийся на двадцати одной конфигурации, пробит.
Причина была не в модели, а в постановке задачи.

| | Spearman | различающий шум | шум / отрыв чемпиона |
|---|---|---|---|
| опубликованный чекпоинт | 0.737 | 2.10e8 ₽ | 2.41 |
| база на верном стенде | +0.526 | 2.22e8 ₽ | 2.93 |
| **ранговый лосс, вес 3, долго** | **+0.745** | 1.23e8 ₽ | 1.61 |
| **ранговый лосс, вес 2, долго** | +0.707 | **1.00e8 ₽** | **1.32** |

Парный бутстрап, 4000 пересэмплирований: **+0.208 Spearman против базы**,
95% интервал [+0.097, +0.331], вероятность улучшения 100%.

### Что сработало — и только вместе

**Ранговый член в лоссе.** Сценарии сравниваются попарно по денежному прокси
прямо при обучении. Без него ранговая корреляция пикует к пятой эпохе и падает,
пока поштатный лосс улучшается ещё тридцать: регрессия по узлам и
упорядочивание сценариев — конфликтующие задачи.

**Сценарный контекст в признаках узла.** Из двадцати одного признака сценарий
различали ровно два, оба λ-производные. Сводка по фонду даёт прямой канал.

**Длительное обучение.** 250–400 эпох вместо 53. Все прежние прогоны упирались
в лимит эпох, а не сходились, и каждый раз я делал вывод по недоученной модели.

По отдельности каждый компонент давал мало или ничего. Контрактная
параметризация целей, длительность и ранговый отбор чекпоинта в прежней
постановке были нейтральны — потому что постановка была неверной.

### Полезное следствие: шортлист сокращается впятеро

`p@10` вырос с 0.60 до 0.80, у веса 2 `regret@5` равен нулю. Раньше чемпион
стоял на 26-м месте из 105, и верификация требовала 26 прогонов OPM. Теперь
хватает пяти — впятеро дешевле.

### Оговорка: абсолютный ЧДД смещён на 2.4 млрд

Ранговый лосс нормирует оценки перед сравнением, поэтому абсолютный уровень не
ограничивает вовсе, и модель уплывает на −5…−9 стандартных отклонений. На
порядок это не влияет ни на копейку (сдвиг сохраняет ранги), но для отчётности
требуется аддитивная калибровка по валидации. Смещение постоянно во всех
семействах сценариев: LEVELS −2.35e9, SHUTDOWN −2.40e9, UNREACHABLE −2.43e9,
CONVERSION −2.09e9.

### Ловушка, которую стоит знать

Денежный прокси допускал обводнённость до 1.5, то есть отрицательную нефть.
Ранговый лосс это нашёл и использовал: поднять сценарий в порядке было проще
через дыру, чем через физику. Обученная так модель дала лучший ранг на
валидации (+0.908) и **−0.512 на тесте**, с чемпионом на 105 месте из 105.
Предел сведён к единице во всех путях и закрыт тестом.

---

## 0.7. Главное 22.08: ранжирование держится целиком на λ, а λ — шум

Двадцать одна конфигурация на верном стенде. Потолок Spearman ≈ 0.53 не
сдвигается ничем внутри модели, а единственное, что его разрушает, — удаление
признаков связности.

### Замер 1: признаки λ и есть весь механизм различения сценариев

| рука | Spearman | p@1 | валидационный лосс |
|---|---|---|---|
| с λ, patience 30 | **+0.529** | 1.00 | 5.2797e-03 |
| **λ обнулена** | **−0.143** | 0.00 | 5.4736e-03 |

Обнуление `cumulative_neighbor_injection_m3` и
`current_neighbor_injection_m3_per_day` — единственных λ-производных в
`_node_vector` — роняет ранговую корреляцию с +0.53 до **−0.14**, при том что
поштатный лосс проседает всего на 3.7%. Модель различает сценарии почти
исключительно через связность; всё остальное — общий фон.

### Замер 2: оценка λ не сходится

Устойчивость между двумя независимыми половинами обучающего сплита, тем же
способом, каким мерилась измеренная λ (у неё 0.993):

| сценариев на половину | устойчивость | средняя разность | среднее значение |
|---|---|---|---|
| 120 | +0.809 | 0.04162 | 0.03802 |
| 240 | +0.584 | 0.02747 | 0.02694 |

Разность между двумя независимыми оценками **равна величине самих значений**,
и с ростом выборки согласие не растёт, а падает. `estimate_training_lambda`
выдаёт шум.

### Что из этого следует

Ранжирование живёт на двух столбцах, которые выводятся из матрицы, чья оценка
не сходится. Это объясняет и потолок, и то, почему его ничем не сдвинуть:

- корреляция ошибок между архитектурами **0.9965–0.9994**, ансамбль из
  четырёх моделей не снижает ошибку вовсе (2.2415e8 против 2.2429e8), 99.9%
  ошибки систематическая — все модели одинаково слепы;
- ширина 128→1024, глубина 3→8, эмбеддинг 16→64, dropout, терпение 10→30,
  остаточные связи, три функции потерь, две параметризации целей — всё в
  полосе Spearman 0.49–0.54.

**Запрос: подать измеренную λ (`data/lambda-window-2007/lambda.json`) в
`build_model_z_context` вместо `estimate_training_lambda`.** По всем замерам
это единственная правка, способная сдвинуть потолок, и это та же болезнь, что
уже вылечена в правилах: там λ была нулевой заглушкой, R1 душил закачку, а
измерение кампанией дало рост ЧДД с 10.805 до 12.678 млрд.

### Побочно: три метрики оказались независимыми

Рука с **лучшим** валидационным лоссом (остаточная сеть, 5.2660e-03) даёт
**почти худший** Spearman (+0.487) и при этом **лучшую** калибровку (наклон
+0.423 против типичных +0.17). Поштатный лосс, сжатие разброса и ранговая
корреляция не связаны между собой. Отбирать чекпоинт по лоссу, надеясь на
ранжирование, нельзя — но и по сжатию тоже.

### Денежное взвешивание отклонено

Пять вариантов, все хуже: Spearman 0.18–0.22 против 0.53, `p@1` ноль,
отрицательный наклон. Механизм: вес в log-пространстве растёт экспоненциально
с величиной, и десятки узлов забирают весь градиент. По умолчанию выключено,
в main вредного не попало.

---

## 1. ✅ Закрыто 22.08 своими силами: A/B на 700 прогонах

**Прогон сделан, машина не понадобилась.** Пункт стоял блокирующим дважды: из-за
записи в `STATUS.md` про 200 машино-часов перегенерации (неверно, раздел 9) и
из-за пика памяти в 22 ГБ. Второе снято двумя правками в `perf/surrogate/tensor-entry`:

- `fit_tensors` — обучение по готовым тензорам, вызывающий строит их потоком и
  освобождает отклик сразу после каждого сценария. Пик 4.3 ГБ вместо 22;
- нарезка батчей срезом вместо `DataLoader` — тот выбирал элементы батча по
  одному и склеивал в Python: 3.66 с против 0.05 с на батче 32768. Больше
  половины эпохи уходило на нарезку, и это же маскировало выигрыш от Metal.

Итого эпоха 9.8 с вместо ~140, рука на 700 прогонах — 10 минут вместо трёх
часов. Девять рук уместились в один день на ноутбуке. Результаты — раздел 0.5.

Заодно найден баг: при разделении `fit` терялось приведение скейлерами, и **ни
один тест этого не ловил** — обучение сходилось и на сырых величинах, просто
хуже. Закрыт отдельным тестом.

## 2. ✅ Закрыто 21.08: какая λ лежит в контексте

Ответ получен, коммит `abb8ff2` в докладах: λ в суррогат попадает, вывод
предыдущего коммита был неверен. Расхождения источников нет, вопрос снят.

---

## 3. Открыто и стало важнее: OOD не подключён к поиску

**Что не так.** `optimizer/schedule_search.py:621` берёт `scored.output` и
**нигде не читает `scored.score`**. Детектор выхода за обучающий диапазон
считается и выбрасывается.

**Почему стало важнее.** G10 показал, что на кандидатах CMA-ES Spearman
предсказания против факта равен 0.0036. Разбор в разделе 4: поиск сошёлся в
область, где модель слепа. Расстояние до обучающей области — единственная
величина, которая об этом сигнализирует, и она уже считается.

**Что запустить.** Прогнать θ\* и базовый план через `predict_with_score`,
напечатать `score` и `worst`. Десять минут.

**Что мне нужно.** Значение `score` для θ\*, для базового плана и для тех 40
кандидатов G10. Если у кандидатов он растёт по ходу сходимости — это прямая
метрика того, как поиск уходит от данных, и её надо ставить в trust region
раньше всего остального.

---

## 4. Переписано 21.08: чем на самом деле занять цикл верификации

**Мой тезис в первой редакции был опровергнут.** Я предлагал поручать
суррогату шортлист, а выбор отдавать Flow, и приводил кривую regret с
обнулением на K=26 из 105. Замер G10 на 40 кандидатах CMA-ES: **Spearman
0.0036**, Pearson 0.157, истинный чемпион на **39 месте из 40**. Порядок
внутри шортлиста модель назначает случайно. Тезис в той формулировке неверен.

**Почему кривая обманула.** Моя строилась на **тестовом сплите суррогата** —
105 сценариев из тех же семейств возмущений, что и обучение, то есть внутри
распределения и с широким разбросом ЧДД. Кандидаты G10 — это **сошедшаяся
популяция CMA-ES**, отобранная самой моделью по высокому предсказанию. Два
разрушительных отличия: выборка смещена по шуму модели, и разброс истинного
ЧДД внутри неё крошечный.

**Это видно прямо в числах G10.** `regret@1 = 32.5 млн` при шуме модели
**210 млн**. Сигнал в шесть раз ниже шума — нулевой Spearman тут арифметика,
а не свойство модели. Никакая точность ранжирования на таком материале не
достижима и не нужна.

**Конструктивный вывод: `regret@1` в 32.5 млн — хорошая новость.** Все 40
кандидатов эквивалентны с точностью до 0.15σ. Выбор между ними не стоит
ничего, и тратить на их упорядочивание прогоны OPM бессмысленно. Настоящие
деньги там, где их нашёл G9, — миллиард на недокачке.

**Что я предлагаю вместо шортлиста.**

1. Бюджет OPM тратить на **разнообразие**, а не на ранжирование: кандидаты,
   разнесённые в θ-пространстве, а не сошедшийся кластер. Отбирать по
   расстоянию друг от друга при приемлемом предсказании, а не по верхушке
   предсказания.
2. Trust region в `optimizer/verification.py` резать **по расстоянию до
   обучающей области** (`scored.score` из пункта 3), а не по величине
   предсказания. Сейчас критерий отбора и источник ошибки — одна и та же
   величина, поэтому отбор систематически выбирает точки максимальной ошибки.
3. Останов поиска по OOD, а не по сходимости предсказания: как только
   популяция ушла за обучающую область, дальнейшие поколения улучшают только
   согласие с моделью.

**Что мне нужно.** `score` для 40 кандидатов G10 (пункт 3) и разброс истинного
ЧДД внутри этих 40 — чтобы посчитать, какого разнесения по θ хватает, чтобы
истинные различия поднялись выше 210 млн шума.

---

## 5. Данные, которые прошу скинуть

- найденная θ\* и 40 кандидатов G10 с предсказанным и фактическим ЧДД
  (`data/g10-verification/table.json`)
- `data/g9-attribution.json` — разложение разрыва
- `data/lambda-window-2007/lambda.json`

---

## 6. ✅ Закрыто 21.08: G9 не требовал нового расчётчика

Подтвердилось: разрыв разложен и сводится точно, отдельный расчётчик не
понадобился. `NpvTable` несёт три разложения — `by_year`, `by_month`,
`by_well` — и инвариант «сумма поскважинных равна `npv_methodology` без
остатка». `tools/g9_diff.py` считает это за секунды.

---

## 7. Что в ветке `feat/surrogate/34-money-loss` (влито в main, `aacab8e`)

192 теста зелёные, старое поведение — дефолт.

- **денежный вес лосса**: `|₽/ед|·scale·(1+v)`, где `(1+v)` — якобиан log1p;
  коэффициенты из `NormativeSet`, не из констант;
- **контрактная параметризация целей**: нефть выводится, а не предсказывается
  (§5.1). Опубликованная модель это требование нарушает — `oil_mass_delta`
  предсказывается независимым таргетом, и на нём висит 97.1% денежного веса;
- **цепное правило** для коэффициентов новой параметризации;
- **сценарный денежный прокси** и ранговая корреляция на проходе валидации;
- флаги `--money-loss-alpha`, `--money-weight-cap`, `--lr-schedule`,
  `--select-by`, `--target-parameterization`.

Проверено на синтетике: денежный лосс снимает сжатие 0.491 → 0.808, `p@1`
0 → 1. Косинусное затухание lr **ухудшает** (0.491 → 0.378) — недообученной
сети нужно больше шагов, а не меньше; отбор по рангу инертен. Оба оставлены
флагами с прежними дефолтами.

Контрактная параметризация на моём стенде дала отрицательный результат
(Spearman 0.351 → 0.321), но стенд негоден — см. пункт 1.

---

## 8. Кеш датасета хранит абсолютные пути сервера

Записи в `dataset-*/cache/*.json` указывают на `/root/aios-hack/aios/...`, а
`RunCache.lookup` объявляет запись недействительной, если файла нет. В
`aios/data/` пути переписаны 21.08. В `../dataset-700/` я переписал их
локально 20.08, но в самом репозитории датасета они не тронуты: у любого, кто
склонирует его по README, все 699 записей мертвы, и генератор молча уходит
пересчитывать в OPM. Чинится переписыванием `artifacts` на пути относительно
корня датасета.

Заодно: в README датасета не упомянут `model-task34-700/model.pt` — по
описанию кажется, что обученной модели там нет.

---

## 9. Главное 21.08: датасет воспроизводим, 200 машино-часов не нужны

В `STATUS.md` записано, что датасет невоспроизводим, канон спецификации
разошёлся и требуется полная перегенерация. **Проверено — неверно.**

**Канон спецификации не расходился.** `spec_hash` базового сценария в
датасете — `721ca9be…`, и ровно столько же даёт сегодняшний код и на `main`,
и на ветке до G2. Значение `240cafa8…` из `STATUS.md` в датасете отсутствует;
похоже, сравнивали с прогоном на устаревшем деке.

**Дек эмитируется байт в байт тот же.** `deck_hash` = `2e7741d9bdf15192…`
совпадает с манифестом. Это решающий довод: вход симулятора идентичен, значит
сохранённые отклики действительны, и пересчитывать нечего.

**Разошёлся только `canonical_schedule_hash`** — учётная треть ключа кеша,
которую менял G2:

```
код до G2:   12 попаданий из 12,  plan_hash 86c74787…  (= в датасете)
код main:     0 попаданий из 12,  plan_hash 9dc1242f…
```

**Значит это переиндексация, а не перегенерация.** Сделана штатным
`RunCache.store`: 502 записи перекованы под новый канон, 198 уже совпадали,
ноль неудач. Старые записи не тронуты, эмиссия деков не нужна, минуты работы.

**Проверено штатным путём** — эмиссия дека, `deck_hashes`, `lookup`:

```
dataset-main         50 попаданий из 50 кодом main
dataset-extra-500    (та же выборка)
```

Осторожно с эмиссией деков при проверке: `emit_deck` копирует 74-мегабайтный
`Model_Z_grid.inc` в каталог каждого сценария, на 700 прогонах это 52 ГБ. Я
на этом переполнил диск. Каталог `decks/` после проверки надо удалять.

**Один открытый вопрос по G2.** Хеш расписания менялся сознательно, но старые
артефакты под него не переносились. Стоит решить, что канон: если новый — в
репозиторий датасета надо положить перекованный кеш, иначе следующий, кто его
склонирует, наступит на то же самое.
