[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aios"
version = "0.0.0"
description = "Мультиагентная система управления фондом скважин — трек 2 AIOS"
requires-python = ">=3.11"
dependencies = [
    # economics/normatives_io.py читает docs/models/CHDD_PYTHON/input/Нормативы_ЧДД.xlsx —
    # источник истины по нормативам. При отсутствии openpyxl загрузчик падает
    # на разбор xlsx как zip+XML силами stdlib, значения те же.
    "openpyxl>=3.1",
]

[project.optional-dependencies]
dev = ["pytest>=8"]
ml = [
    "numpy>=2.0",
    "torch>=2.6",
]

# Явный список всех Python-пакетов полного интегрированного репозитория.
# Новые подпакеты нужно добавлять сюда, иначе они не попадут в установку и образ.
[tool.setuptools]

[tool.setuptools.packages.find]
where = ["src"]
include = [
    "aios_backend*",
]
exclude = ["*.tests", "tests*"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = [
    "tests",
    "src/aios_backend/infrastructure/opm/tests",
    "src/aios_backend/domain/configuration/tests",
    "src/aios_backend/domain/connectivity/tests",
    "src/aios_backend/core/contracts/tests",
    "src/aios_backend/domain/economics/tests",
    "src/aios_backend/infrastructure/llm/tests",
    "src/aios_backend/application/optimization/tests",
    "src/aios_backend/domain/policy/tests",
    "src/aios_backend/domain/robustness/tests",
    "src/aios_backend/domain/schedule/tests",
    "src/aios_backend/ml/surrogate/tests",
    "src/aios_backend/presentation/ui_export/tests",
]
