# AIOS — трек 2, кодовая репа

Инструкции для агента, работающего в этом репозитории. Прочитать перед первой правкой кода.

## Первое действие в сессии

Эта репа — не единственная. Рядом должна быть склонирована `aios-hack/docs` (сиблинг-каталог, `../docs` от корня этой репы) — там задача, контракты с обоснованиями и персональные карточки. Если её нет — сказать пользователю и остановиться, работать без нее нельзя: карточки заданий и `docs/context/08_contracts.md` живут только там.

Дальше: **спросить пользователя, если не сказано явно, чья это сессия** — Савелий, Андрей, Иван или Михаил. Открыть `../docs/assignments/<имя>.md` и прочитать целиком. Это список задач с приёмкой, порядком старта и ловушками, лично под эту роль.

## Порядок чтения, обязательный

1. `../docs/assignments/<имя>.md` — что делать и в каком порядке.
2. `contracts/README.md` (этот каталог) — точная форма всех общих типов: имена полей, единицы, оси, инварианты.
3. `contracts/*.py` — те же типы, но исполняемые. **Импортировать их, не переизобретать.** `from contracts import Schedule, StateAtDate, IntervalResponse, ...` — весь публичный API в `contracts/__init__.py`.
4. Разделы `../docs/context/08_contracts.md`, на которые ссылается карточка (§N) — там объяснено, почему контракт именно такой, а не просто какой он.

Если контракт неясен из `contracts/*.py` и `contracts/README.md` — смотреть `docs/context/08_contracts.md`, не гадать и не придумывать своё поле.

## У ЧДД есть эталонная реализация — сверяться с ней, а не с интуицией

`../docs/models/CHDD_PYTHON/` — расчётчик организаторов, считающий «по той же методике, что применяется при оценке». Проверяющая сторона считает **этим кодом**, поэтому расхождение с ним — наш дефект по определению, спорить не о чем. Формат его входа (`REQUIRED_COLUMNS` в `chdd_model.py`) задаёт состав нашей выгрузки из симулятора, а `input/Нормативы_ЧДД.xlsx` — источник значений нормативов.

Контракты синхронизированы с ним 15.08. Четыре вещи до этой даты были в них неверны, и если увидишь их в чужом коде или в старой ветке — это ошибка:

| Неверно | Верно |
|---|---|
| `FCF = EBITDA − CAPEX` | `FCF = EBITDA − налог_на_прибыль − CAPEX`, налог 25% на **годовой** базе |
| Перевод под закачку `5.0 млн + ЭЦН` | Ровно `5.0 млн`, целиком в OPEX, ЭЦН не добавляется |
| Первый ЭЦН вводимой скважины в CAPEX | Первичное оснащение не начисляется вовсе |
| Нормативы годовым рядом (`by_year`) | Один скалярный набор на весь горизонт |

Плюс два правила, которых раньше не было нигде: потолок `SET_LRAT` в 500 м³/сут (`MAX_LRAT_M3_PER_DAY`, проверяется конструктором `ControlEvent`) и исключение строк с отрицательным месячным приростом (`is_excluded_by_negative_rule`). Второе применять обязательно, даже когда собственные приросты заведомо неотрицательны — почему, написано в докстринге функции.

## Установка

```bash
cd aios
python3 -m venv .venv && .venv/bin/pip install -e ".[dev]"
.venv/bin/pytest contracts/tests   # контракты уже проверены, все зелёные — стартовая точка
```

Свой пакет (`schedule/`, `bridge/`, `economics/`, ...) создавать рядом с `contracts/`, добавлять в `[tool.setuptools] packages` в `pyproject.toml` своей строкой — не трогая чужие.

## Жёсткие правила репозитория

1. **`contracts/` правит только интегратор — Савелий.** Всем остальным: нужна правка типа — заводится issue, самовольно не редактировать. Если контракта не хватает для задачи — не подгонять под себя, остановиться и сообщить, что нужна правка контракта.
2. **Каждый пишет только в своих каталогах**, даже ради однострочной правки в чужом. Список — `README.md` в этом каталоге.
3. **Моков нет.** Компонент, который не может посчитать, бросает исключение (`NotImplementedError` — законно). `return 0.0`, `return None`, случайный тензор нужной формы — запрещены. Единственное исключение из правила — `RunResult`: падение симулятора это статус, не exception.
4. **Синтетические данные** — только для проверки формы и сериализации, никогда в метриках качества, и помечаются в метаданных.
5. **Производное не хранится.** Типоразмер ЭЦН, стоимости событий, ЧДД — вычисляются, не лежат полем рядом с исходными данными.
6. **Ветки `feat/<имя>/<номер-задачи>`**, PR в `main`. Не ребейзить чужие ветки.
7. **Приёмка задачи — тест в своём каталоге.** Что именно проверяется, написано в карточке задачи (`docs/assignments/<имя>.md`) в колонке «Приёмка» — это и есть содержание теста, а не вольная трактовка.

## Две шкалы времени — самая частая ошибка проекта

Дек живёт на 371 дате (`deck_date_index` 0…370), управление — на 225 датах (`control_step` 0…224), но **подтверждённых интервалов только 224** (`control_step` 0…223) — у шага 224 нет наблюдаемого месяца и он не несёт решений. Оба типа отклика (`StateAtDate` — по дате дека, `IntervalResponse` — по интервалу управления) уже разведены в `contracts/response.py` ровно для того, чтобы эту ошибку нельзя было сделать на уровне типов. Не сворачивать их обратно в один тензор.

Полная форма — `contracts/README.md` §1.2.

## Если что-то в контракте противоречит другому месту в контракте

Не выбирать самому, какая версия правильная. `docs/context/08_contracts.md` содержит журнал решений (`context/07_concept.md` §9) — искать там дату последнего изменения этого куска и опираться на неё. Если после этого всё ещё неоднозначно — сообщить пользователю, не гадать.
